mutation deleteEvent($id: ID!) {
  deleteEvent(id: $id) {
    ...EventFragment
  }
}

mutation createEvent($input: CreateEventInput!) {
  createEvent(input: $input) {
    event {
      ...EventFragment
    }
    errors {
      ...ValidationErrorFragment
    }
  }
}

mutation updateEvent($id: ID!, $input: UpdateEventInput!) {
  updateEvent(id: $id, input: $input) {
    event {
      ...EventFragment
    }
    errors {
      ...ValidationErrorFragment
    }
  }
}

mutation addEventManager($eventId: ID!, $userId: ID!) {
  addEventManager(eventId: $eventId, userId: $userId) {
    ...EventFragment
  }
}

mutation removeEventManager($eventId: ID!, $userId: ID!) {
  removeEventManager(eventId: $eventId, userId: $userId) {
    ...EventFragment
  }
}

mutation addTeamToEvent($eventId: ID!, $teamId: ID!) {
  addTeamToEvent(eventId: $eventId, teamId: $teamId) {
    ...EventFragment
  }
}

mutation removeTeamFromEvent($eventId: ID!, $teamId: ID!) {
  removeTeamFromEvent(eventId: $eventId, teamId: $teamId) {
    ...EventFragment
  }
}
