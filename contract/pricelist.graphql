# import * from "./shared/scalars.graphql"
# import * from "./shared/types.graphql"

type PricelistEntry implements INode {
  id: ID!
  "Product name"
  n: String!
  "Unit"
  u: String
  "Unit price"
  up: Float!
}
